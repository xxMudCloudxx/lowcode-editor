{
  "extends": "../tsconfig.json", // 继承根目录的通用配置
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext", // 关键：使用 Node.js 最新的模块系统
    "moduleResolution": "NodeNext", // 关键：配套的模块解析策略
    "lib": ["ES2022"], // 关键：只包含 ES 标准库，不包含 DOM
    "composite": true, // 允许被根 tsconfig 引用
    "noEmit": true, // 脚本文件我们通常不希望 tsc 输出 js，由 ts-node 或其他工具直接执行
    "isolatedModules": false // 脚本文件不需要此限制
  },
  "jsx": "react-jsx",
  "types": ["node", "react", "react-dom"],
  "skipLibCheck": true,
  "include": [
    "./**/*.ts" // 这个 tsconfig 只管理当前目录下的所有 .ts 文件
  ]
}
