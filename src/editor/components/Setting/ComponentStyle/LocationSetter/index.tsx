import { Divider } from "antd";

interface LocationSetterProps {}

const LocationSetter = (props: LocationSetterProps) => {
  return (
    <div>
      <Divider orientation="left">位置</Divider>
    </div>
  );
};

export default LocationSetter;
